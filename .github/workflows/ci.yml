name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  train-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run training script
        run: |
          python src/train.py
      
      - name: Build Docker image
        run: |
          docker build -t mlops-api:${{ github.sha }} .
          docker build -t mlops-api:latest .
      
      # Optional: Upload model to GCS (uncomment if using cloud storage)
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v1
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}
      # - name: Set up Cloud SDK
      #   uses: google-github-actions/setup-gcloud@v1
      # - name: Upload model to GCS
      #   run: |
      #     gsutil cp artifacts/model.pkl gs://${{ secrets.GCS_BUCKET_NAME }}/models/model-${{ github.sha }}.pkl
      #     gsutil cp artifacts/model.pkl gs://${{ secrets.GCS_BUCKET_NAME }}/models/model-latest.pkl

